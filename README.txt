Run arrange-dir_install.exe AS ADMINISTRATOR.

Warning: Do not move individually, delete or rename 
these files or you may crash my program and impossible 
the automatic uninstall of the files by the uninstaller
and impossible the automatic installation.

The uninstaller is going to clean everything up.
Run the uninstaller as administrator or the remove
will not be successful, and for it to be successful
download/copy the uninstaller again (because it
self-destroys).

arrange-dir program parameters:
arrange-dir [directory]

arrange-dir.exe will move to C:\Windows\System32 when
the installer is run.


If you think you are pro enough, for manual
uninstalltion you can read the .reg files, delete
the registers and delete the program manually.